[build-system]
requires = ["setuptools >= 61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "solid_octo_fishstick"
version = "0.0.1"

dependencies = [
  "numpy", "urllib", "PIL","pandas",
]
